# 開発プロセス

実装の指示を受けたら、以下のプロセスに従う:

1. 対応する GitHub Issue を作成する
2. 対応するブランチを作成し、その中で作業する
3. Plan モードで実装方針を定める
4. 実装完了後、ユーザーにブラウザ上で動作確認してもらう（マージ前に必ず確認を取る）
5. ユーザーから修正・追加実装の指示があれば対応する
6. ユーザーから OK が出たら、PR 作成 → マージ → ブランチ削除 → mainをpullまで行う
